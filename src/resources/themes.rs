// Code generated by Scalar SDK Generator. DO NOT EDIT.
//! The `themes` resource.

#[derive(Clone)]
pub struct Themes {
    client: crate::client::Scalar,
}

impl Themes {
    pub(crate) fn new(client: crate::client::Scalar) -> Self {
        Self { client }
    }

    /// List all themes
    pub fn list(&self) -> ListRequest {
        ListRequest::new(self.client.clone())
    }

    /// Create a theme
    pub fn create(&self, body: crate::models::ThemesCreateBody) -> CreateRequest {
        CreateRequest::new(self.client.clone(), body)
    }

    /// Update theme metadata
    pub fn update(&self, slug: impl Into<String>, body: crate::models::ThemesUpdateBody) -> UpdateRequest {
        UpdateRequest::new(self.client.clone(), slug.into(), body)
    }

    /// Update theme document
    pub fn replace_document(&self, slug: impl Into<String>, body: crate::models::ThemesReplaceDocumentBody) -> ReplaceDocumentRequest {
        ReplaceDocumentRequest::new(self.client.clone(), slug.into(), body)
    }

    /// Delete a theme
    pub fn delete(&self, slug: impl Into<String>) -> DeleteRequest {
        DeleteRequest::new(self.client.clone(), slug.into())
    }

    /// Get a theme
    pub fn retrieve(&self, slug: impl Into<String>) -> RetrieveRequest {
        RetrieveRequest::new(self.client.clone(), slug.into())
    }
}

/// List all team themes.
#[must_use = "a request builder does nothing until `.send().await` is called"]
pub struct ListRequest {
    client: crate::client::Scalar,
}

impl ListRequest {
    fn new(client: crate::client::Scalar) -> Self {
        Self {
            client,
        }
    }

    /// Sends the request and returns the decoded response.
    pub async fn send(self) -> Result<Vec<crate::models::Theme>, crate::error::Error> {
        let path = "/v1/themes".to_string();
        let query: Vec<(String, String)> = Vec::new();
        let headers: Vec<(String, String)> = Vec::new();
        let body: Option<&serde_json::Value> = None;
        self.client
            .send::<Vec<crate::models::Theme>, _>(reqwest::Method::GET, &path, &query, &headers, body, true)
            .await
    }
}
/// Create a team theme.
#[must_use = "a request builder does nothing until `.send().await` is called"]
pub struct CreateRequest {
    client: crate::client::Scalar,
    body: crate::models::ThemesCreateBody,
    idempotency_key: Option<String>,
}

impl CreateRequest {
    fn new(client: crate::client::Scalar, body: crate::models::ThemesCreateBody) -> Self {
        Self {
            client,
            body,
            idempotency_key: None,
        }
    }

    /// Sets an idempotency key so this request can be safely retried.
    pub fn idempotency_key(mut self, value: impl Into<String>) -> Self {
        self.idempotency_key = Some(value.into());
        self
    }

    /// Sends the request and returns the decoded response.
    pub async fn send(self) -> Result<crate::models::Uid, crate::error::Error> {
        let path = "/v1/themes".to_string();
        let query: Vec<(String, String)> = Vec::new();
        let mut headers: Vec<(String, String)> = Vec::new();
        if let Some(key) = self.idempotency_key {
            headers.push(("Idempotency-Key".to_string(), key));
        }
        let body = Some(&self.body);
        self.client
            .send::<crate::models::Uid, _>(reqwest::Method::POST, &path, &query, &headers, body, true)
            .await
    }
}
/// Update theme metadata.
#[must_use = "a request builder does nothing until `.send().await` is called"]
pub struct UpdateRequest {
    client: crate::client::Scalar,
    slug: String,
    body: crate::models::ThemesUpdateBody,
    idempotency_key: Option<String>,
}

impl UpdateRequest {
    fn new(client: crate::client::Scalar, slug: String, body: crate::models::ThemesUpdateBody) -> Self {
        Self {
            client,
            slug,
            body,
            idempotency_key: None,
        }
    }

    /// Sets an idempotency key so this request can be safely retried.
    pub fn idempotency_key(mut self, value: impl Into<String>) -> Self {
        self.idempotency_key = Some(value.into());
        self
    }

    /// Sends the request and returns the decoded response.
    pub async fn send(self) -> Result<serde_json::Value, crate::error::Error> {
        let path = format!("/v1/themes/{}", crate::http::encode_path_param(&self.slug));
        let query: Vec<(String, String)> = Vec::new();
        let mut headers: Vec<(String, String)> = Vec::new();
        if let Some(key) = self.idempotency_key {
            headers.push(("Idempotency-Key".to_string(), key));
        }
        let body = Some(&self.body);
        self.client
            .send::<serde_json::Value, _>(reqwest::Method::PATCH, &path, &query, &headers, body, true)
            .await
    }
}
/// Replace the theme document.
#[must_use = "a request builder does nothing until `.send().await` is called"]
pub struct ReplaceDocumentRequest {
    client: crate::client::Scalar,
    slug: String,
    body: crate::models::ThemesReplaceDocumentBody,
    idempotency_key: Option<String>,
}

impl ReplaceDocumentRequest {
    fn new(client: crate::client::Scalar, slug: String, body: crate::models::ThemesReplaceDocumentBody) -> Self {
        Self {
            client,
            slug,
            body,
            idempotency_key: None,
        }
    }

    /// Sets an idempotency key so this request can be safely retried.
    pub fn idempotency_key(mut self, value: impl Into<String>) -> Self {
        self.idempotency_key = Some(value.into());
        self
    }

    /// Sends the request and returns the decoded response.
    pub async fn send(self) -> Result<serde_json::Value, crate::error::Error> {
        let path = format!("/v1/themes/{}", crate::http::encode_path_param(&self.slug));
        let query: Vec<(String, String)> = Vec::new();
        let mut headers: Vec<(String, String)> = Vec::new();
        if let Some(key) = self.idempotency_key {
            headers.push(("Idempotency-Key".to_string(), key));
        }
        let body = Some(&self.body);
        self.client
            .send::<serde_json::Value, _>(reqwest::Method::PUT, &path, &query, &headers, body, true)
            .await
    }
}
/// Delete a theme by slug.
#[must_use = "a request builder does nothing until `.send().await` is called"]
pub struct DeleteRequest {
    client: crate::client::Scalar,
    slug: String,
    idempotency_key: Option<String>,
}

impl DeleteRequest {
    fn new(client: crate::client::Scalar, slug: String) -> Self {
        Self {
            client,
            slug,
            idempotency_key: None,
        }
    }

    /// Sets an idempotency key so this request can be safely retried.
    pub fn idempotency_key(mut self, value: impl Into<String>) -> Self {
        self.idempotency_key = Some(value.into());
        self
    }

    /// Sends the request and returns the decoded response.
    pub async fn send(self) -> Result<serde_json::Value, crate::error::Error> {
        let path = format!("/v1/themes/{}", crate::http::encode_path_param(&self.slug));
        let query: Vec<(String, String)> = Vec::new();
        let mut headers: Vec<(String, String)> = Vec::new();
        if let Some(key) = self.idempotency_key {
            headers.push(("Idempotency-Key".to_string(), key));
        }
        let body: Option<&serde_json::Value> = None;
        self.client
            .send::<serde_json::Value, _>(reqwest::Method::DELETE, &path, &query, &headers, body, true)
            .await
    }
}
/// Get the theme document by slug.
#[must_use = "a request builder does nothing until `.send().await` is called"]
pub struct RetrieveRequest {
    client: crate::client::Scalar,
    slug: String,
}

impl RetrieveRequest {
    fn new(client: crate::client::Scalar, slug: String) -> Self {
        Self {
            client,
            slug,
        }
    }

    /// Sends the request and returns the decoded response.
    pub async fn send(self) -> Result<bytes::Bytes, crate::error::Error> {
        let path = format!("/v1/themes/{}", crate::http::encode_path_param(&self.slug));
        let query: Vec<(String, String)> = Vec::new();
        let headers: Vec<(String, String)> = Vec::new();
        let body: Option<&serde_json::Value> = None;
        self.client
            .send_bytes(reqwest::Method::GET, &path, &query, &headers, body, true)
            .await
    }
}
