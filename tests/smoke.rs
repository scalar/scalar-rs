// Code generated by Scalar SDK Generator. DO NOT EDIT.
//! Runtime smoke test: drives every synthesizable operation once.
//!
//! Set `SCALAR_BASE_URL` (and any credential env vars) to point the client at a
//! server and actually send requests; with it unset the test is a no-op, so a
//! bare `cargo test` just confirms the generated surface compiles.
#![allow(unused)]

use scalar_sdk::*;

#[tokio::test]
async fn smoke() {
    if std::env::var("SCALAR_BASE_URL").is_err() {
        return;
    }
    let client = Scalar::from_env().expect("client builds from environment");
    let mut failures: Vec<String> = Vec::new();
    {
        let result: Result<(), Error> = async {
            let _ = client.registry().list_all_api_documents().send().await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!("{}: {error}", "GET /v1/apis"));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client.registry().list_api_documents("example").send().await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!("{}: {error}", "GET /v1/apis/{namespace}"));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client
                .registry()
                .create_api_document(
                    "example",
                    RegistryCreateApiDocumentBody {
                        title: "".to_string(),
                        description: None,
                        version: "x".to_string(),
                        slug: "".to_string(),
                        ruleset: None,
                        is_private: None,
                        document: "".to_string(),
                    },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!("{}: {error}", "POST /v1/apis/{namespace} [required params]"));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client
                .registry()
                .create_api_document(
                    "example",
                    RegistryCreateApiDocumentBody {
                        title: "".to_string(),
                        description: Some("".to_string()),
                        version: "x".to_string(),
                        slug: "".to_string(),
                        ruleset: Some("".to_string()),
                        is_private: Some(false),
                        document: "".to_string(),
                    },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!("{}: {error}", "POST /v1/apis/{namespace} [all params]"));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client
                .registry()
                .update_api_document(
                    "example",
                    "example",
                    RegistryUpdateApiDocumentBody {
                        title: None,
                        description: None,
                        is_private: None,
                        ruleset: None,
                    },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!(
                    "{}: {error}",
                    "PATCH /v1/apis/{namespace}/{slug} [required params]"
                ));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client
                .registry()
                .update_api_document(
                    "example",
                    "example",
                    RegistryUpdateApiDocumentBody {
                        title: Some("".to_string()),
                        description: Some("".to_string()),
                        is_private: Some(false),
                        ruleset: Some("".to_string()),
                    },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!("{}: {error}", "PATCH /v1/apis/{namespace}/{slug} [all params]"));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client
                .registry()
                .delete_api_document("example", "example")
                .send()
                .await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!("{}: {error}", "DELETE /v1/apis/{namespace}/{slug}"));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client
                .registry()
                .retrieve_api_document_version("example", "example", "example")
                .send()
                .await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!(
                    "{}: {error}",
                    "GET /v1/apis/{namespace}/{slug}/version/{semver}"
                ));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client
                .registry()
                .update_api_document_version(
                    "example",
                    "example",
                    "example",
                    RegistryUpdateApiDocumentVersionBody {
                        document: "".to_string(),
                        last_known_version_sha: None,
                    },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!(
                    "{}: {error}",
                    "PATCH /v1/apis/{namespace}/{slug}/version/{semver} [required params]"
                ));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client
                .registry()
                .update_api_document_version(
                    "example",
                    "example",
                    "example",
                    RegistryUpdateApiDocumentVersionBody {
                        document: "".to_string(),
                        last_known_version_sha: Some("".to_string()),
                    },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!(
                    "{}: {error}",
                    "PATCH /v1/apis/{namespace}/{slug}/version/{semver} [all params]"
                ));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client
                .registry()
                .delete_api_document_version("example", "example", "example")
                .send()
                .await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!(
                    "{}: {error}",
                    "DELETE /v1/apis/{namespace}/{slug}/version/{semver}"
                ));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client
                .registry()
                .list_api_document_version_metadata("example", "example", "example")
                .send()
                .await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!(
                    "{}: {error}",
                    "GET /v1/apis/{namespace}/{slug}/version/{semver}/metadata"
                ));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client
                .registry()
                .create_api_document_version(
                    "example",
                    "example",
                    RegistryCreateApiDocumentVersionBody {
                        version: "x".to_string(),
                        document: "".to_string(),
                        force: None,
                        last_known_version_sha: None,
                    },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!(
                    "{}: {error}",
                    "POST /v1/apis/{namespace}/{slug}/version [required params]"
                ));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client
                .registry()
                .create_api_document_version(
                    "example",
                    "example",
                    RegistryCreateApiDocumentVersionBody {
                        version: "x".to_string(),
                        document: "".to_string(),
                        force: Some(false),
                        last_known_version_sha: Some("".to_string()),
                    },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!(
                    "{}: {error}",
                    "POST /v1/apis/{namespace}/{slug}/version [all params]"
                ));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client
                .registry()
                .create_api_document_access_group(
                    "example",
                    "example",
                    AccessGroup {
                        access_group_slug: "xxx".to_string(),
                    },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!("{}: {error}", "POST /v1/apis/{namespace}/{slug}/access-group"));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client
                .registry()
                .delete_api_document_access_group(
                    "example",
                    "example",
                    AccessGroup {
                        access_group_slug: "xxx".to_string(),
                    },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!(
                    "{}: {error}",
                    "DELETE /v1/apis/{namespace}/{slug}/access-group"
                ));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client.schemas().list("example").send().await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!("{}: {error}", "GET /v1/schemas/{namespace}"));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client
                .schemas()
                .create(
                    "example",
                    SchemasCreateBody {
                        title: "".to_string(),
                        description: None,
                        version: "x".to_string(),
                        slug: "".to_string(),
                        is_private: None,
                        document: "".to_string(),
                    },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!("{}: {error}", "POST /v1/schemas/{namespace} [required params]"));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client
                .schemas()
                .create(
                    "example",
                    SchemasCreateBody {
                        title: "".to_string(),
                        description: Some("".to_string()),
                        version: "x".to_string(),
                        slug: "".to_string(),
                        is_private: Some(false),
                        document: "".to_string(),
                    },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!("{}: {error}", "POST /v1/schemas/{namespace} [all params]"));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client
                .schemas()
                .update(
                    "example",
                    "example",
                    SchemasUpdateBody {
                        title: None,
                        description: None,
                        is_private: None,
                    },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!(
                    "{}: {error}",
                    "PATCH /v1/schemas/{namespace}/{slug} [required params]"
                ));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client
                .schemas()
                .update(
                    "example",
                    "example",
                    SchemasUpdateBody {
                        title: Some("".to_string()),
                        description: Some("".to_string()),
                        is_private: Some(false),
                    },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!(
                    "{}: {error}",
                    "PATCH /v1/schemas/{namespace}/{slug} [all params]"
                ));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client.schemas().delete("example", "example").send().await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!("{}: {error}", "DELETE /v1/schemas/{namespace}/{slug}"));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client
                .schemas()
                .version()
                .retrieve("example", "example", "example")
                .send()
                .await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!(
                    "{}: {error}",
                    "GET /v1/schemas/{namespace}/{slug}/version/{semver}"
                ));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client
                .schemas()
                .version()
                .delete("example", "example", "example")
                .send()
                .await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!(
                    "{}: {error}",
                    "DELETE /v1/schemas/{namespace}/{slug}/version/{semver}"
                ));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client
                .schemas()
                .version()
                .create(
                    "example",
                    "example",
                    VersionCreateBody {
                        version: "x".to_string(),
                        document: "".to_string(),
                    },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!("{}: {error}", "POST /v1/schemas/{namespace}/{slug}/version"));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client
                .schemas()
                .access_group()
                .create(
                    "example",
                    "example",
                    AccessGroup {
                        access_group_slug: "xxx".to_string(),
                    },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!(
                    "{}: {error}",
                    "POST /v1/schemas/{namespace}/{slug}/access-group"
                ));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client
                .schemas()
                .access_group()
                .delete(
                    "example",
                    "example",
                    AccessGroup {
                        access_group_slug: "xxx".to_string(),
                    },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!(
                    "{}: {error}",
                    "DELETE /v1/schemas/{namespace}/{slug}/access-group"
                ));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client.login_portals().retrieve("example").send().await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!("{}: {error}", "GET /v1/login-portals/{slug}"));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client
                .login_portals()
                .update("example", LoginPortalsUpdateBody { title: None })
                .send()
                .await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!(
                    "{}: {error}",
                    "PATCH /v1/login-portals/{slug} [required params]"
                ));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client
                .login_portals()
                .update(
                    "example",
                    LoginPortalsUpdateBody {
                        title: Some("".to_string()),
                    },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!("{}: {error}", "PATCH /v1/login-portals/{slug} [all params]"));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client.login_portals().delete("example").send().await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!("{}: {error}", "DELETE /v1/login-portals/{slug}"));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client
                .login_portals()
                .create(LoginPortalsCreateBody {
                    title: "".to_string(),
                    slug: "".to_string(),
                    email: LoginPortalEmail {
                        logo: "".to_string(),
                        logo_size: "100".to_string(),
                        button_text: "Login".to_string(),
                        message: "Click to access private documentation hosted by scalar.com".to_string(),
                        title: "Private Docs".to_string(),
                        main_color: "#2a2f45".to_string(),
                        main_background: "#f6f6f6".to_string(),
                        card_color: "2a2f45".to_string(),
                        card_background: "#fff".to_string(),
                        button_color: "#fff".to_string(),
                        button_background: "#0f0f0f".to_string(),
                    },
                    page: LoginPortalPage {
                        title: "Scalar Private Docs".to_string(),
                        description: "Login to access your documentation".to_string(),
                        head: "".to_string(),
                        script: "".to_string(),
                        theme: "".to_string(),
                        company_name: "".to_string(),
                        logo: "".to_string(),
                        logo_url: "".to_string(),
                        favicon: "".to_string(),
                        terms_link: "".to_string(),
                        privacy_link: "".to_string(),
                        form_title: "Scalar Private Docs".to_string(),
                        form_description: "Login to access your documentation".to_string(),
                        form_image: "".to_string(),
                    },
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!("{}: {error}", "POST /v1/login-portals"));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client.login_portals().list().send().await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!("{}: {error}", "GET /v1/login-portals"));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client.rules().list_rulesets("example").send().await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!("{}: {error}", "GET /v1/rulesets/{namespace}"));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client
                .rules()
                .create_ruleset(
                    "example",
                    RulesCreateRulesetBody {
                        title: "".to_string(),
                        description: None,
                        slug: "".to_string(),
                        is_private: None,
                        document: "".to_string(),
                    },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!(
                    "{}: {error}",
                    "POST /v1/rulesets/{namespace} [required params]"
                ));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client
                .rules()
                .create_ruleset(
                    "example",
                    RulesCreateRulesetBody {
                        title: "".to_string(),
                        description: Some("".to_string()),
                        slug: "".to_string(),
                        is_private: Some(false),
                        document: "".to_string(),
                    },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!("{}: {error}", "POST /v1/rulesets/{namespace} [all params]"));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client
                .rules()
                .update_ruleset(
                    "example",
                    "example",
                    RulesUpdateRulesetBody {
                        namespace: None,
                        slug: None,
                        title: None,
                        description: None,
                        is_private: None,
                    },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!(
                    "{}: {error}",
                    "PATCH /v1/rulesets/{namespace}/{slug} [required params]"
                ));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client
                .rules()
                .update_ruleset(
                    "example",
                    "example",
                    RulesUpdateRulesetBody {
                        namespace: Some("".to_string()),
                        slug: Some("".to_string()),
                        title: Some("".to_string()),
                        description: Some("".to_string()),
                        is_private: Some(false),
                    },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!(
                    "{}: {error}",
                    "PATCH /v1/rulesets/{namespace}/{slug} [all params]"
                ));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client.rules().delete_ruleset("example", "example").send().await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!("{}: {error}", "DELETE /v1/rulesets/{namespace}/{slug}"));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client
                .rules()
                .retrieve_ruleset_document("example", "example")
                .send()
                .await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!("{}: {error}", "GET /v1/rulesets/{namespace}/{slug}"));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client
                .rules()
                .create_ruleset_access_group(
                    "example",
                    "example",
                    AccessGroup {
                        access_group_slug: "xxx".to_string(),
                    },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!(
                    "{}: {error}",
                    "POST /v1/rulesets/{namespace}/{slug}/access-group"
                ));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client
                .rules()
                .delete_ruleset_access_group(
                    "example",
                    "example",
                    AccessGroup {
                        access_group_slug: "xxx".to_string(),
                    },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!(
                    "{}: {error}",
                    "DELETE /v1/rulesets/{namespace}/{slug}/access-group"
                ));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client.themes().list().send().await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!("{}: {error}", "GET /v1/themes"));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client
                .themes()
                .create(ThemesCreateBody {
                    name: "".to_string(),
                    description: None,
                    slug: "".to_string(),
                    document: "".to_string(),
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!("{}: {error}", "POST /v1/themes [required params]"));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client
                .themes()
                .create(ThemesCreateBody {
                    name: "".to_string(),
                    description: Some("".to_string()),
                    slug: "".to_string(),
                    document: "".to_string(),
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!("{}: {error}", "POST /v1/themes [all params]"));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client
                .themes()
                .update(
                    "example",
                    ThemesUpdateBody {
                        name: None,
                        description: None,
                    },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!("{}: {error}", "PATCH /v1/themes/{slug} [required params]"));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client
                .themes()
                .update(
                    "example",
                    ThemesUpdateBody {
                        name: Some("".to_string()),
                        description: Some("".to_string()),
                    },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!("{}: {error}", "PATCH /v1/themes/{slug} [all params]"));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client
                .themes()
                .replace_document(
                    "example",
                    ThemesReplaceDocumentBody {
                        document: "".to_string(),
                    },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!("{}: {error}", "PUT /v1/themes/{slug}"));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client.themes().delete("example").send().await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!("{}: {error}", "DELETE /v1/themes/{slug}"));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client.themes().retrieve("example").send().await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!("{}: {error}", "GET /v1/themes/{slug}"));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client.teams().list().send().await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!("{}: {error}", "GET /v1/teams"));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client.scalar_docs().list_guides().send().await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!("{}: {error}", "GET /v1/guides"));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client
                .scalar_docs()
                .create_guide(ScalarDocsCreateGuideBody {
                    name: "".to_string(),
                    slug: None,
                    is_private: false,
                    allowed_users: vec![],
                    allowed_domains: vec![],
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!("{}: {error}", "POST /v1/guides [required params]"));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client
                .scalar_docs()
                .create_guide(ScalarDocsCreateGuideBody {
                    name: "".to_string(),
                    slug: Some("xxx".to_string()),
                    is_private: false,
                    allowed_users: vec![],
                    allowed_domains: vec![],
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!("{}: {error}", "POST /v1/guides [all params]"));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client.scalar_docs().publish_guide("example").send().await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!("{}: {error}", "POST /v1/guides/{slug}/publish"));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client.namespaces().list().send().await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!("{}: {error}", "GET /v1/namespaces"));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client
                .authentication()
                .exchange_personal_token(AuthenticationExchangePersonalTokenBody {
                    personal_token: "".to_string(),
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!("{}: {error}", "POST /v1/auth/exchange"));
            }
        }
    }
    {
        let result: Result<(), Error> = async {
            let _ = client.authentication().list_current_user().send().await?;
            Ok(())
        }
        .await;
        if let Err(error) = result {
            if is_smoke_failure(&error) {
                failures.push(format!("{}: {error}", "GET /v1/auth/me"));
            }
        }
    }
    assert!(failures.is_empty(), "smoke failures:\n{}", failures.join("\n"));
}

/// A smoke failure is a plumbing problem — the request could not be sent or
/// the client was misconfigured. An API error (a response with a non-success
/// status) or a decode error means the request reached the server and came
/// back, which is what this smoke verifies; the response body itself is the
/// server's concern, not the SDK's.
fn is_smoke_failure(error: &Error) -> bool {
    matches!(
        error,
        Error::Transport(_) | Error::Config(_) | Error::MissingParameter(_)
    )
}
